<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DIY Tracker - Project Hub</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* ------------------------- */
        /* --- MODERN DESIGN SYSTEM --- */
        /* ------------------------- */
        :root {
            --color-primary: #0077b6;
            --color-secondary: #00b4d8;
            --color-accent: #ffb703;
            --color-dark: #111827;
            --color-light: #f7f9fc;
            --color-surface: #ffffff;
            --color-text: #212529;
            --color-text-light: #6c757d;
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.05);
            --shadow-md: 0 6px 15px rgba(0, 0, 0, 0.08);
            --shadow-lg: 0 12px 35px rgba(0, 0, 0, 0.12);
            --border-radius: 12px;
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        /* ------------------------- */
        /* --- GLOBAL & TYPOGRAPHY --- */
        /* ------------------------- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--color-light);
            color: var(--color-text);
            line-height: 1.7;
            min-height: 100vh;
        }

        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }

        /* ------------------------- */
        /* --- GLASSMORPHISM NAVBAR --- */
        /* ------------------------- */
        .navbar {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: var(--color-primary);
            text-decoration: none;
        }

        .navbar-menu a {
            color: var(--color-text-light);
            text-decoration: none;
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: var(--transition);
        }
        
        .navbar-menu a:hover {
            color: var(--color-primary);
            background-color: rgba(0, 119, 182, 0.05);
        }
        
        .navbar-menu .btn-primary-ghost {
            border: 2px solid var(--color-primary);
            color: var(--color-primary);
            font-weight: 600;
        }
        
        .navbar-menu .btn-primary-ghost:hover {
            background-color: var(--color-primary);
            color: #fff;
        }

        /* ------------------------- */
        /* --- DYNAMIC HERO SECTION --- */
        /* ------------------------- */
        .hero {
            background: linear-gradient(135deg, rgba(247, 249, 252, 0.8), rgba(247, 249, 252, 0.7)),
                        url('https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?ixlib=rb-4.0.3&auto=format&fit=crop&w=1500&q=80');
            background-size: cover;
            background-position: center;
            padding: 6rem 2rem;
            text-align: center;
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 700;
            color: var(--color-dark);
            margin-bottom: 1rem;
        }

        .hero-subtitle {
            font-size: 1.25rem;
            color: var(--color-text-light);
            margin-bottom: 2rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .btn-hero {
            background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
            color: white;
            padding: 0.8rem 2.5rem;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            box-shadow: 0 4px 20px rgba(0, 119, 182, 0.3);
            transition: var(--transition);
        }
        
        .btn-hero:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 25px rgba(0, 119, 182, 0.4);
        }

        /* ------------------------- */
        /* --- PROJECT CARDS GRID --- */
        /* ------------------------- */
        .projects-section {
            padding: 4rem 0;
        }

        .section-header {
            text-align: center;
            margin-bottom: 3rem;
        }
        
        .section-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
        }

        .project-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background: var(--color-surface);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-md);
            overflow: hidden;
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            text-decoration: none;
            color: var(--color-text);
        }

        .project-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }
        
        .card-image-wrapper {
            height: 220px;
            overflow: hidden;
        }
        
        .card-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .project-card:hover .card-image {
            transform: scale(1.08);
        }
        
        .card-body {
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .card-text {
            font-size: 0.9rem;
            color: var(--color-text-light);
            flex-grow: 1;
            margin-bottom: 1.5rem;
        }
        
        .card-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
            color: var(--color-text-light);
        }

        .card-meta span {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .card-footer {
            padding: 1rem 1.5rem;
            background-color: var(--color-light);
            border-top: 1px solid #e9ecef;
            display: flex;
            justify-content: flex-end;
            gap: 0.5rem;
        }
        
        .btn-card {
            font-size: 0.8rem;
            font-weight: 600;
            padding: 0.4rem 0.8rem;
            border-radius: 8px;
            text-decoration: none;
            border: none;
            transition: var(--transition);
        }

        .btn-edit {
            background-color: rgba(255, 183, 3, 0.15);
            color: var(--color-accent);
        }
        .btn-edit:hover { background-color: var(--color-accent); color: #fff; }

        .btn-delete {
            background-color: rgba(220, 53, 69, 0.1);
            color: #dc3545;
            cursor: pointer;
        }
        .btn-delete:hover { background-color: #dc3545; color: #fff; }

        /* ------------------------- */
        /* --- FOOTER --- */
        /* ------------------------- */
        .footer {
            background-color: var(--color-dark);
            color: rgba(255, 255, 255, 0.7);
            padding: 3rem 0;
            text-align: center;
            margin-top: 4rem;
        }
        
        .social-icons a {
            color: rgba(255, 255, 255, 0.7);
            margin: 0 0.75rem;
            font-size: 1.3rem;
            text-decoration: none;
            transition: var(--transition);
        }
        
        .social-icons a:hover {
            color: var(--color-secondary);
            transform: translateY(-3px);
        }
        
        .footer-links {
            margin: 1.5rem 0;
        }
        
        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            margin: 0 1rem;
            font-weight: 500;
            transition: var(--transition);
        }
        
        .footer-links a:hover {
            color: #fff;
        }

        /* ------------------------- */
        /* --- RESPONSIVE STYLES --- */
        /* ------------------------- */
        @media (max-width: 992px) {
            .hero-title { font-size: 2.8rem; }
        }

        @media (max-width: 768px) {
            .hero-title { font-size: 2.2rem; }
            .hero { padding: 4rem 1rem; }
            .section-header h1 { font-size: 2rem; }
            .navbar-menu { gap: 0.5rem; }
            .navbar-menu a { padding: 0.4rem 0.8rem; }
        }
    </style>
</head>
<body>

    <nav class="navbar">
        <div class="container navbar-container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-tools"></i> DIY Tracker
            </a>
            <div class="navbar-menu">
                <% if (userId) { %>
                    <a href="/projects/list">All Projects</a>
                    <a href="/logout">Logout</a>
                <% } else { %>
                    <a href="/login">Login</a>
                    <a href="/signup" class="btn-primary-ghost">Signup</a>
                <% } %>
            </div>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-content">
            <h1 class="hero-title">Your Creative Project Hub</h1>
            <p class="hero-subtitle">Track, manage, and showcase your DIY projects with ease and style.</p>
            <a href="/projects/new" class="btn-hero"><i class="fas fa-plus"></i> Start a New Project</a>
        </div>
    </section>

    <main class="projects-section">
        <div class="container">
            <div class="section-header">
                <h1>📋 Latest Projects</h1>
                <a href="/projects/list" style="text-decoration: none;">View All Projects &rarr;</a>
            </div>

            <!-- <div class="project-grid">
                <% if (typeof projects !== 'undefined' && projects.length > 0) { %>
                    <% projects.forEach(project => { %>
                        <div class="project-card">
                            <a href="/projects/<%= project._id %>" class="text-decoration-none">
                                <div class="card-image-wrapper">
                                    <% if (project.imagePath) { %>
                                        <img src="<%= project.imagePath %>" class="card-image" alt="Project Image">
                                    <% } else { %>
                                        <img src="https://images.unsplash.com/photo-1589939705384-5185137a7f0f?q=80&w=1000" class="card-image" alt="Placeholder">
                                    <% } %>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title"><%= project.title %></h5>
                                    <p class="card-text"><%= project.description.substring(0, 100) %>...</p>
                                    <div class="card-meta">
                                        <span>
                                            <i class="fas fa-info-circle"></i> 
                                            <%= project.status || 'Not Started' %>
                                        </span>
                                        <span>
                                            <i class="fas fa-calendar-alt"></i> 
                                            <%= project.dueDate ? new Date(project.dueDate).toLocaleDateString() : 'N/A' %>
                                        </span>
                                    </div>
                                </div>
                            </a>
                            <div class="card-footer">
                                <a href="/projects/<%= project._id %>/edit" class="btn-card btn-edit"><i class="fas fa-pencil-alt"></i> Edit</a>
                                <form action="/projects/<%= project._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this project?');" style="display: contents;">
                                    <button type="submit" class="btn-card btn-delete"><i class="fas fa-trash-alt"></i> Delete</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div style="grid-column: 1 / -1; text-align: center;">
                        <p>No projects yet. Click “Start a New Project” to begin your creative journey!</p>
                    </div>
                <% } %>
            </div> -->
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-links">
                <a href="#">Home</a>
                <a href="/projects/list">Projects</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
            </div>
            <div class="social-icons">
                <a href="#" aria-label="Facebook"><i class="fab fa-facebook"></i></a>
                <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
                <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
                <a href="#" aria-label="Pinterest"><i class="fab fa-pinterest"></i></a>
            </div>
            <p>&copy; <%= new Date().getFullYear() %> DIY Project Tracker. Crafted with <i class="fas fa-heart" style="color: #e74c3c;"></i> in Pune.</p>
        </div>
    </footer>

</body>
</html>