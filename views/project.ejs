<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= project.title %> - DIY Project</title>
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Body & General */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f6fa;
      margin: 0;
      padding: 0;
    }

    /* Navbar */
    .navbar {
      background-color: #343a40 !important;
      padding: 0.8rem 1rem;
    }

    .navbar-brand {
      font-weight: 600;
      font-size: 1.4rem;
    }

    .navbar .btn-outline-light {
      transition: all 0.2s ease;
    }

    .navbar .btn-outline-light:hover {
      color: #343a40;
      background-color: #fff;
      transform: scale(1.05);
    }

    /* Card */
    .card {
      border-radius: 15px;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
    }

    .card-img-top {
      height: 300px;
      object-fit: cover;
      transition: transform 0.5s ease;
    }

    .card-img-top:hover {
      transform: scale(1.05);
    }

    .card-body h1 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .card-body p {
      font-size: 1rem;
      line-height: 1.5;
    }

    .card-footer {
      background-color: #f8f9fa;
      border-top: none;
    }

    .card-footer .btn {
      transition: all 0.2s ease;
      border-radius: 8px;
      min-width: 80px;
    }

    .card-footer .btn:hover {
      transform: scale(1.05);
    }

    .btn-warning {
      background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
      border: none;
      color: #fff;
    }

    .btn-warning:hover {
      filter: brightness(1.1);
    }

    .btn-danger {
      background: linear-gradient(135deg, #f85032 0%, #e73827 100%);
      border: none;
      color: #fff;
    }

    .btn-danger:hover {
      filter: brightness(1.1);
    }

    .btn-secondary {
      background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
      border: none;
      color: #343a40;
    }

    .btn-secondary:hover {
      filter: brightness(1.1);
    }

    /* Footer */
    footer.footer {
      padding: 15px 0;
      background-color: #343a40;
      color: #fff;
      font-size: 0.9rem;
      text-align: center;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .card-img-top {
        height: 220px;
      }

      .card-body h1 {
        font-size: 1.5rem;
      }

      .card-body p {
        font-size: 0.95rem;
      }

      .btn {
        font-size: 0.85rem;
      }
    }

    @media (max-width: 576px) {
      .card-img-top {
        height: 180px;
      }

      .card-body h1 {
        font-size: 1.3rem;
      }

      .card-body p {
        font-size: 0.9rem;
      }

      .btn {
        font-size: 0.8rem;
        padding: 0.3rem 0.6rem;
      }
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">DIY Tracker</a>
      <a href="/projects/list" class="btn btn-outline-light btn-sm ms-auto">All Projects</a>
    </div>
  </nav>

  <!-- Project Card -->
  <div class="container py-4">
    <div class="card shadow-sm">
      <% if (project.imagePath) { %>
        <img src="<%= project.imagePath %>" class="card-img-top" alt="Project Image">
      <% } else { %>
        <img src="https://via.placeholder.com/600x300?text=No+Image" class="card-img-top" alt="Placeholder">
      <% } %>

      <div class="card-body">
        <h1 class="card-title mb-3"><%= project.title %></h1>
        <p class="card-text mb-2"><%= project.description %></p>
        <p class="mb-1"><strong>Requirements:</strong> <%= project.materials %></p>
        <p class="mb-1"><strong>Status:</strong> <%= project.status || 'Not Started' %></p>
        <p class="mb-1"><strong>Progress:</strong> <%= project.progress || 0 %> %</p>
        <p class="mb-1"><strong>Start Date:</strong> <%= project.startDate ? project.startDate.toISOString().split('T')[0] : 'N/A' %></p>
        <p class="mb-1"><strong>Due Date:</strong> <%= project.dueDate ? project.dueDate.toISOString().split('T')[0] : 'N/A' %></p>
      </div>

      <div class="card-footer d-flex justify-content-between flex-wrap gap-2">
        <a href="/projects/<%= project._id %>/edit" class="btn btn-warning">Edit</a>
        <form action="/projects/<%= project._id %>?_method=DELETE" method="POST">
          <button class="btn btn-danger">Delete</button>
        </form>
        <a href="/projects/list" class="btn btn-secondary">Back</a>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <!-- <footer class="footer mt-4 text-center">
    &copy; <%= new Date().getFullYear() %> DIY Project Tracker
  </footer> -->
  <!-- Footer -->
  <footer class="footer">
    <div class="footer-content">
      <p>&copy; 2023 DIY Project Tracker</p>
      <div class="social-icons">
        <a href="#" class="social-icon"><i class="fab fa-facebook"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-twitter"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
        <a href="#" class="social-icon"><i class="fab fa-pinterest"></i></a>
      </div>
    </div>
  </footer>
</body>
</html>
